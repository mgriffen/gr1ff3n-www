# gr1ff3n-www

Homelab Control Center - A cyberpunk-themed dashboard for managing homelab services.

## Development

This is a static website. You can run it locally using Python's built-in HTTP server:

```bash
python3 -m http.server
```

Open [http://localhost:8000](http://localhost:8000) in your browser.
